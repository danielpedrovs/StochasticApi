function isOverSold(stockH14P,stockL14P,ClosePrice){
  const kStock = 100*((ClosePrice-stockL14P)/(stockH14P-stockL14P));
console.log('the %k  is ' + kStock);
const sttmentK = kStock < 20 ? "That means stock is oversold and good to buy" :"That means stock is not oversold, check the K if its over 80 you can sell"; 
console.log(sttmentK);

}
isOverSold();

